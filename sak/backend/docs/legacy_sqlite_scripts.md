# Scripts eliminados

Los antiguos scripts utilitarios heredados de SQLite se eliminaron del directorio `backend` al migrar el proyecto a PostgreSQL.

Si necesitas consultarlos, recuperalos desde el historial de git (rama `server` original o commits previos a su eliminacion).
